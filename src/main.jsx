import React from "react";
import ReactDOM from "react-dom/client";
import TokenTrail from "./TokenTrail.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TokenTrail />
  </React.StrictMode>
);
